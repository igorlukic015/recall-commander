---
description: Run the full test suite and summarize results
---
Run `dotnet test` across all test projects and summarize pass/fail counts per
project. If anything fails, show the failing test output and fix the cause
(or explain it if it is a genuine regression the change intended).
